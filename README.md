# ClassCore Website

Express website and API for ClassCore, including the landing page, downloads page, contact form, security headers, and download metadata API.

## Run locally

```powershell
npm install
npm start
```

Open `http://localhost:3000`.

## Deploy free

See [DEPLOYMENT.md](DEPLOYMENT.md). The included `render.yaml` configures a free Render web service.
